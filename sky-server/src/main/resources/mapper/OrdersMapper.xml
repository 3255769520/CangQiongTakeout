<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hhxy.mapper.OrdersMapper">

    <select id="sumTurnoverByDate" resultType="java.math.BigDecimal">
        SELECT SUM(amount) AS total_turnover
        FROM orders
        WHERE status = 5
          AND order_time >= #{begin}
          AND order_time &lt;= #{end}
    </select>

    <select id="getTurnoverByDate" resultType="java.util.HashMap">
        SELECT
            DATE(order_time) AS order_date,
            SUM(amount) AS daily_turnover
        FROM orders
        WHERE status = 5
          AND order_time >= #{begin}
          AND order_time &lt;= #{end}
        GROUP BY DATE(order_time)
        ORDER BY order_date ASC
    </select>

</mapper>